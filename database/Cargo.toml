[package]
name = "database"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = {version = "1.0", features = ["derive"] }
serde_json = "1.0"
mongodb = { version = "1.2.0", default-features = false, features = ["sync"]}